// Package model holds the shared domain types used across adapters, classifier,
// emitters, cache, and the UI: lineup nouns (channel, programme, provider) and
// emitted artifacts (M3U/XMLTV files). Types may carry self-contained business
// logic that only needs the model itself.
package model
