package model

// M3UFile is a rendered #EXTM3U playlist document (bytes on the wire / on disk).
type M3UFile []byte

// XMLTVFile is a rendered XMLTV guide document (bytes on the wire / on disk).
type XMLTVFile []byte
