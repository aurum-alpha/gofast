/bin/
web/node_modules/
# Local runtime data volume (docker-compose bind mount for /data:
# config.yaml, cache/ generations, channelattr/). Never commit.
/.data/
# Legacy local bind mounts (pre single-/data volume).
/.cache/
/.channelattr/
